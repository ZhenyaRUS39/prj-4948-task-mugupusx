# Plan: task-mugupusx

**Project:** `prj-618-pil-script`  
**Task ID:** `task-mugupusx`  
**Repo:** `prj-4948-task-mugupusx`  
**Progress:** 1/5 subtasks done

## Summary

Декомпозиция задачи на PIL-скрипт: создание репо, реализация скрипта обработки изображений, юнит-тесты, документация. Стек Python+Pillow, репо prj-4948-task-mugupusx.

## Subtasks

### ❌ 1. Инициализировать репозиторий и структуру

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Status:** `failed`
- **Description:** Создать локальный клон репо prj-4948-task-mugupusx, добавить .gitignore (Python), requirements.txt (Pillow, pytest), пустой README.md и директорию src/ и tests/.
- **Test plan:** git status чистый; файлы requirements.txt, .gitignore, README.md, директории src/ и tests/ существуют; python -c 'import sys; print(sys.version)' работает.

### ⬜ 2. Реализовать image_processor.py

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Status:** `pending`
- **Description:** В src/image_processor.py реализовать функции: resize_image(input_path, output_path, width, height), convert_format(input_path, output_path, target_format), apply_grayscale(input_path, output_path), rotate_image(input_path, output_path, angle). Все функции принимают пути и обрабатывают ошибки (FileNotFoundError, UnsupportedFormat).
- **Test plan:** python -c 'from src.image_processor import resize_image, convert_format, apply_grayscale, rotate_image' импорт успешен; dry-run на тестовом PNG создаёт выходной файл нужного формата/размера.
- **Dependencies:** `sub-1`

### ⬜ 3. Реализовать CLI-обёртку main.py

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Status:** `pending`
- **Description:** В src/main.py реализовать CLI с argparse: подкоманды resize/convert/grayscale/rotate с параметрами (--input, --output, --width, --height, --angle, --format). Точка входа if __name__ == '__main__'.
- **Test plan:** python src/main.py resize --help выводит справку; python src/main.py grayscale --input tests/fixtures/test.png --output /tmp/out.png создаёт grayscale-изображение.
- **Dependencies:** `sub-2`

### ⬜ 4. Написать юнит-тесты pytest

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Status:** `pending`
- **Description:** В tests/test_image_processor.py покрыть все 4 функции: resize корректно меняет размер, convert конвертирует PNG->JPEG, grayscale делает изображение монохромным (mode 'L'), rotate поворачивает на заданный угол. Использовать фикстуры с PIL.Image.new для генерации тестовых изображений. Покрытие ≥80%.
- **Test plan:** pytest tests/ -v проходит все тесты зелёным; pytest --cov=src --cov-report=term-missing показывает покрытие ≥80%.
- **Dependencies:** `sub-2`

### ⬜ 5. Написать README.md с примерами использования

- **ID:** `sub-5`
- **Profile:** `30razrab2`
- **Status:** `pending`
- **Description:** Описать установку (pip install -r requirements.txt), все CLI-команды с примерами, формат входных/выходных файлов, поддерживаемые форматы (PNG/JPEG/BMP/WEBP), лицензию MIT.
- **Test plan:** README.md содержит секции Installation, Usage (с примерами для всех 4 подкоманд), Supported Formats; ссылки на примеры воспроизводимы по документации.
- **Dependencies:** `sub-3`, `sub-4`


---
*Generated by Hermes Orchestrator*
